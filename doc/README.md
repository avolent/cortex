# cortex docs

This folder is the cortex project's own documentation — and it doubles as a
demo of how cortex renders a folder of markdown.

Run `cortex -dir doc` from the repo root to see this folder served as a wiki
at `http://localhost:8090`.

## Contents

- [Architecture](architecture.md) — how cortex is built, request flow, live reload
- [Rules](rules.md) — the hard project principles
- [Usage](usage.md) — flags, behaviour, examples
